<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>ContractManagement.Domain</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- Domain layer has no dependencies on other projects or external frameworks -->
  </ItemGroup>

</Project>